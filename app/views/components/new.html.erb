<h1>Add Component</h1>

<%= form_for(@component, url: experiment_components_path(@experiment)) do |f| %>
  <p>
  	<%= f.label :type_of %>
  	<%= f.text_field :type_of %>
  </p>

  <p>
  	<%= f.label :body %>
  	<%= f.text_area :body %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>